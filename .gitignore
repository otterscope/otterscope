# binaries
/otterscope
/dist/

# data
*.db
*.db-wal
*.db-shm

# frontend — dist is embedded via go:embed, so .gitkeep must exist for
# `go build ./...` to compile without a prior frontend build
web/node_modules/
web/dist/*
!web/dist/.gitkeep

# editor/os
.idea/
.vscode/
.DS_Store
